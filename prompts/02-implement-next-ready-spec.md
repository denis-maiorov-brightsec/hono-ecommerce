You are the implementation agent for this repository.

Tasks:
1. Read `AGENTS.md`, `docs/STACK_PROFILE.md`, and `docs/SPECS_INDEX.md`.
2. Pick exactly one `Ready` spec from `docs/specs/`.
3. Use ContextPlus MCP for code discovery before editing:
   - Start with `mcp__contextplus__semantic_code_search` and/or `mcp__contextplus__semantic_identifier_search` to locate relevant files and symbols.
   - Use `mcp__contextplus__get_file_skeleton` and `mcp__contextplus__get_context_tree` to understand structure before making changes.
4. Implement only that spec.
5. Add/update tests required by acceptance criteria.
6. Run lint and tests for touched areas.
7. Update `docs/SPECS_INDEX.md` status for that spec.
8. Commit using `feat(spec-00x): ...`, `refactor(spec-00x): ...`, `test(spec-00x): ...`, or `docs(spec-00x): ...`.

Return:
- What changed
- Acceptance criteria checklist
- Test/lint commands executed and results
- Risks or follow-up notes

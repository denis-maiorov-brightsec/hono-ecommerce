You are the implementation agent for this repository.

Tasks:
1. Read `AGENTS.md`, `docs/STACK_PROFILE.md`, and `docs/SPECS_INDEX.md`.
2. Pick exactly one `Ready` spec from `docs/specs/`.
3. Implement only that spec.
4. Add/update tests required by acceptance criteria.
5. Run lint and tests for touched areas.
6. Update `docs/SPECS_INDEX.md` status for that spec.
7. Commit using `feat(spec-00x): ...`, `refactor(spec-00x): ...`, `test(spec-00x): ...`, or `docs(spec-00x): ...`.

Return:
- What changed
- Acceptance criteria checklist
- Test/lint commands executed and results
- Risks or follow-up notes
